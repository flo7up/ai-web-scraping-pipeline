Domain: {{domainDescription}}
Record type: {{recordType}}
Schema fields:
{{schemaJson}}
Allowed domains:
{{allowedDomains}}
Seed URLs:
{{seedUrls}}
Maximum query count: {{maxQueryCount}}

Generate search queries that can discover public pages for this pipeline. Prefer precise phrases and domain-specific terms. If allowed domains are provided, include useful `site:` queries.
